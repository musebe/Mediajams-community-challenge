export default async (req, res) => {
  if (req.method === 'POST') {
    /**
     * POST METHOD
     * @description Write a post method that will upload an Image to Cloudinary when the "Upload File" button is       clicked
     */
  }
};
